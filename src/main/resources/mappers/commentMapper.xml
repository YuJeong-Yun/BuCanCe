<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcc.mapper.CommentMapper">
	<!-- 댓글 조회 -->
	<select id="readComment" resultType="CommentVO">
		SELECT
		CNO, CONTENT, WRITER,
		REGDATE, VISIT
<<<<<<< HEAD
		FROM bcc_comment
=======
		FROM comment
>>>>>>> 63a9e5a7cb363781a17497c9c15347ee59fa38a6
		WHERE num = #{num}
	</select>
	<!-- 댓글 작성 -->
<<<<<<< HEAD
	<insert id="writeComment">
		insert into bcc_comment(num, content, writer, visit)
=======
	<insert id="createComment">
		insert into comment(num, content, writer, visit)
>>>>>>> 63a9e5a7cb363781a17497c9c15347ee59fa38a6
		value(#{num}, #{content}, #{writer}, #{visit})
	</insert>
	<!-- 댓글 수정 -->
	<update id="update" parameterType="com.bcc.domain.CommentVO">
		update comment set content
		=
		#{content}
		where cno = #{cno}
	</update>
	<!-- 댓글 삭제 -->
	<delete id="delete">
		DELETE FROM comment
		WHERE cno = #{cno}
	</delete>
	
	<!-- 선택된 댓글 조회 -->
	<select id="selectComment" resultType="com.bcc.domain.CommentVO">
	select num, cno, content, writer, regdate from comment where cno = #{cno}
	</select>
	<!-- 댓글 수 -->
	<update id="updateCommentCnt" parameterType="int"> update tourist_spot
		b set b.commentCnt = (select count(cno) from comment where num =
		#{num})
		where b.num = #{num}
	</update>
	
</mapper>