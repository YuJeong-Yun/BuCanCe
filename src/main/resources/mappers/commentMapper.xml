<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bcc.mapper.CommentMapper">


	<!-- 댓글 조회 -->
	<select id="readComment" resultType="CommentVO">
		SELECT
		CNO, CONTENT, WRITER,
		REGDATE
		FROM comment
		WHERE num = #{num}
	</select>

	<!-- 댓글 작성 -->
	<insert id="writeComment">
		insert into comment(num, content, writer)
		value(#{num}, #{content}, #{writer})
	</insert>

	<!-- 댓글 수정 -->
	<update id="modify" parameterType="com.bcc.domain.CommentVO">
		update comment set content
		= #{content}
		where cno = #{cno}
	</update>

	<!-- 댓글 삭제 -->
	<delete id="delete">
	DELETE FROM comment
  		WHERE cno = #{cno}
	</delete>

	<!-- 선택된 댓글 조회 -->
	<select id="selectComment" resultType="com.bcc.domain.CommentVO">
		select num, cno, content,
		writer, regdate from comment
		where cno = #{cno}
	</select>
	
	<!-- 방문 미방분 분류 -->
	<select id="visit" resultType="com.bcc.domain.CommentVO">
	
	</select>

</mapper>