<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.bcc.mapper.memberMapper">
  
  	<select id="getNow" resultType="string">
  		select now() 
  	</select>
<<<<<<< HEAD
  	
  	<insert id="insertPay" parameterType="PreOrderVO">
		insert into premium_order
		values (#{id},systimestamp,systdate+30,sysdate+29,#{pm})
	</insert>
	


	
<<<<<<<< HEAD:src/main/resources/mappers/memberMapper.xml
=======

    <select id="countMember" resultType="int">
      select count(*) from bcc_member
    </select>
    
    <insert id="insertMember">
    	insert into bcc_member (id, pw, name, email, tel, zip, address1, address2) 
    	values(#{id}, #{pw}, #{name}, #{email}, #{tel}, #{zip}, #{address1}, #{address2})    
    </insert>
    
    <select id="login" resultType="com.bcc.domain.MemberVO">
        select * from bcc_member 
        where id=#{id} and pw=#{pw}
    </select>
    
    <update id="update">
       update bcc_member 
       set email=#{email}, pw=#{pw}, tel=#{tel}
       where id=#{id}
    </update>
    
    <delete id="delete">
      delete from bcc_member 
      where id=#{id} and pw=#{pw}
    </delete>
    
	<select id="getMember" resultType="com.bcc.domain.MemberVO">
		select * from bcc_member 
		where id = #{id}
	</select>
	
	<select id="memberList" resultType="MemberVO">
	  select * from bcc_member 
	  where id != #{id}	
	</select>
	
	<select id="idCheck" parameterType="String" resultType="int">
		select count(id) 
		from bcc_member 
		where id = #{id}
    </select>

	<update id="licenseUp">
		update bcc_member
		set license=1
		where id=#{id} 
	</update>
	
>>>>>>> 8bfabfee6c031d19cb1177d51d9ff50f77e6a7f4
	<update id="licenseDown">
		update bcc_member
		set license=0
		where id=#{id} 
	</update>
<<<<<<< HEAD

	<select id="thumbList" resultType="BoardVO">
        select tourist_spot.*
		from tourist_spot left join thumb
		on tourist_spot.num = thumb.b_num and tourist_spot.t_category = thumb.b_category
        where thumb.m_id=#{id }
		order by tourist_spot.num asc
	</select>
	
    <delete id="deleteThumb" parameterType="int">
		delete from thumb
		where b_num = #{b_num}
    </delete>
	
	<!-- 카카오 로그인 -->
	<!-- 정보 찾기 -->
	<select id="findKakao" parameterType="java.util.HashMap" resultType="KakaoVO">
		select * from kakao_table
		where k_name=#{nickname} and k_email=#{email}
	</select>

	<!-- 정보 저장 -->
	<insert id="kakaoInsert" parameterType="java.util.HashMap">
		insert into kakao_table(k_name, k_email)
		values(#{nickname}, #{email})
	</insert>
	<!-- 카카오 로그인 -->
	
	<!-- 네이버 로그인 -->
	<!-- 정보 찾기 -->
	<select id="findNaver" parameterType="java.util.HashMap" resultType="NaverVO">
		select * from naver_table
		where n_name=#{nickname} and n_email=#{email}
	</select>

	<!-- 정보 저장 -->
	<insert id="naverInsert" parameterType="java.util.HashMap">
		insert into naver_table(n_name, n_email)
		values(#{nickname}, #{email})
	</insert>
	<!-- 네이버 로그인 -->
	
========
  	
  	
>>>>>>>> 8bfabfee6c031d19cb1177d51d9ff50f77e6a7f4:src/main/resources/mappers/testMapper.xml
=======
  	
>>>>>>> 8bfabfee6c031d19cb1177d51d9ff50f77e6a7f4
  </mapper>
  